import Link from "next/link";
export default async function SuccessPage({ searchParams }: { searchParams: Promise<{ pedido?: string }> }) { const order = (await searchParams).pedido; return <main className="container-page grid min-h-[65vh] place-content-center text-center"><p className="eyebrow text-[var(--gold)]">Pedido recibido</p><h1 className="display mt-3 text-5xl md:text-6xl">Gracias por elegir MJ Vault</h1>{order && <p className="mt-5">Tu número de pedido es <strong>{order}</strong>.</p>}<p className="muted mx-auto mt-3 max-w-lg leading-7">Continúa la conversación en WhatsApp para confirmar disponibilidad, envío y forma de pago.</p><Link className="button-primary mx-auto mt-7" href="/tienda">Seguir explorando</Link></main>; }

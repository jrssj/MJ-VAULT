import { notFound } from "next/navigation";
import { MessageCircle } from "lucide-react";
import { requireAdmin } from "@/lib/auth";
import { updateOrderStatusAction } from "@/app/admin/actions";
import { formatCurrency, formatDate } from "@/lib/format";
import { getSettings } from "@/lib/data";
export default async function OrderPage({params}:{params:Promise<{id:string}>}) { const {id}=await params; const {supabase}=await requireAdmin(); const [{data:order},settings]=await Promise.all([supabase.from("orders").select("*, order_items(*)").eq("id",id).single(),getSettings()]); if(!order)notFound(); const statuses=["PENDIENTE_CONTACTO","CONTACTADO","CONFIRMADO","PAGADO","ENVIADO","ENTREGADO","CANCELADO"]; const number=settings?.whatsapp_number||process.env.NEXT_PUBLIC_WHATSAPP_NUMBER; const message=encodeURIComponent(`Hola ${order.customer_name}, te contactamos de MJ Vault por tu pedido ${order.order_number}.`); return <><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow text-[var(--gold)]">{formatDate(order.created_at)}</p><h1 className="display mt-2 text-5xl">Pedido {order.order_number}</h1></div>{number&&<a className="button-primary" href={`https://wa.me/${String(number).replace(/\D/g,"")}?text=${message}`} target="_blank" rel="noreferrer"><MessageCircle size={17}/>Contactar por WhatsApp</a>}</div><div className="mt-8 grid gap-6 xl:grid-cols-[1fr_360px]"><section className="border hairline bg-[var(--paper)]"><h2 className="display border-b hairline p-5 text-3xl">Productos</h2><div>{order.order_items.map((item:{id:string;product_name_snapshot:string;variant_snapshot:{size?:string;color?:string};quantity:number;unit_price:number;subtotal:number})=><div key={item.id} className="flex justify-between gap-4 border-b hairline p-5 text-sm"><div><strong>{item.quantity} × {item.product_name_snapshot}</strong><p className="muted mt-1">{[item.variant_snapshot?.color,item.variant_snapshot?.size].filter(Boolean).join(" · ")||"Sin variante"}</p><p className="muted mt-1">{formatCurrency(item.unit_price)} c/u</p></div><strong>{formatCurrency(item.subtotal)}</strong></div>)}</div><div className="flex justify-between p-5"><span>Subtotal</span><strong>{formatCurrency(order.subtotal)}</strong></div></section><aside className="space-y-6"><section className="border hairline bg-[var(--paper)] p-5"><h2 className="display text-3xl">Estado</h2><form action={updateOrderStatusAction.bind(null,id)} className="mt-5 grid gap-3"><select name="status" defaultValue={order.status} className="input">{statuses.map(status=><option key={status}>{status}</option>)}</select><button className="button-primary">Actualizar estado</button></form></section><section className="border hairline bg-[var(--paper)] p-5"><h2 className="display text-3xl">Cliente</h2><dl className="mt-5 grid gap-3 text-sm"><div><dt className="muted text-xs">Nombre</dt><dd>{order.customer_name}</dd></div><div><dt className="muted text-xs">Teléfono</dt><dd>{order.phone}</dd></div><div><dt className="muted text-xs">Entrega</dt><dd>{order.delivery_method}</dd></div><div><dt className="muted text-xs">Dirección</dt><dd>{order.address}, {order.neighborhood}<br/>{order.city}, {order.department}</dd></div>{order.notes&&<div><dt className="muted text-xs">Notas</dt><dd>{order.notes}</dd></div>}</dl></section></aside></div></>; }
